# -*- coding:Utf-8 -*-

class File_analysis(object):
    """
    Representation of a file and manages all computations ad operations required.
    """

    def __init__(self, file_location=""):
        """
        Steps :
        - read the file in a string
        - browse the file char by char -> count the chars, count the char bigrams, count the char trigrams, count the mean size of words
        - split the string in words -> count the words, count the word bigrams, count the word trigram
        - todo zipf law
        """
        print('file location : ' + file_location)





